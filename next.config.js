/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["picsum.photos", "upload.ocwsb.com", "upload.fandika.live"],
  },
}

module.exports = nextConfig
